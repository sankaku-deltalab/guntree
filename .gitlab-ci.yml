image: node:latest

cache:
  paths:
    - node_modules/

stages:
  - test
  - deploy

test:
  stage: test
  script:
    - npm install
    - npm test

pages:
  stage: deploy
  script:
    - npm install
    - npm run generate-document
  artifacts:
    paths:
      - public
  only:
      - master
